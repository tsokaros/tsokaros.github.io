
<!-- _includes/projects.html -->
<style>
.project-card {
  background-color: rgb(3, 21, 51); /* Specific RGB color */
  border-radius: 10px; /* Rounded edges */
  transition: background-color 0.3s ease, transform 0.3s ease, z-index 0.3s ease; /* Smooth transition animation */
  position: relative; /* Ensure proper positioning */
  display: flex; /* Use flexbox layout */
}

.project-card:hover {
  background-color: rgb(74, 77, 130); /* Adjusted color on hover */
  transform: scale(1.08); /* Slight scale increase on hover */
  z-index: 1; /* Bring the hovered card to the top */
}

.card-img {
  flex: 0 0 auto; /* Allow image to maintain its original size */
  overflow: hidden; /* Hide any overflowing content */
  border-radius: 10px 0 0 10px; /* Rounded corners for left side */
}

.card-img img {
  width: 100%; /* Ensure image fills its container */
  height: auto; /* Automatically adjust height while maintaining aspect ratio */
}

.card-body {
  flex: 1; /* Allow body to take remaining space */
  padding: 1rem; /* Add padding */
}

.card-text {
  color: white; /* Set text color */
}

.github-icon {
  margin-top: auto; /* Align GitHub icon to bottom */
}

.icon {
  display: inline-block; /* Ensure icon is inline */
  margin-right: 5px; /* Add some spacing */
}

.stars {
  color: white; /* Set color for stars */
  display: inline-block; /* Ensure stars are inline */
  margin-left: 10px; /* Add some spacing */
}
</style>

<div class="card-item col">
  {%- if project.redirect -%}
  <a href="{{ project.redirect }}">
  {%- else -%}
  <a href="{{ project.url | relative_url }}">
  {%- endif -%}
    <div class="project-card card hoverable">
      {%- if project.img -%}
      <div class="card-img col-md-6">
        {% include figure.html path=project.img alt="project thumbnail" %}
      </div>
      {%- endif -%}
      <div class="col-md-6">
        <div class="card-body">
          <p class="card-text">{{ project.description }}</p>
          <div class="row ml-1 mr-1 p-0">
            {%- if project.github -%}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ project.github }}"><i class="fab fa-github gh-icon"></i></a>
              </div>
              {%- if project.github_stars -%}
              <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                <i class="fas fa-star"></i>
                <span id="{{ project.github_stars }}-stars"></span>
              </span>
              {%- endif -%}
            </div>
            {%- endif -%}
          </div>
        </div>
      </div>
    </div>
  </a>
</div>
